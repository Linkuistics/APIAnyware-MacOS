# Target: racket-oo

Racket OO bindings using the `tell` macro and class wrappers for idiomatic
Racket object-oriented interaction with macOS APIs.

## Before working here

> Paths below are project-root-relative.

1. Read `knowledge/targets/racket-oo.md` for target-wide learnings
2. Check `LLM_STATE/targets/racket-oo/backlog.md` for current progress

## Target structure

- `crate/` — Rust emitter crate (emit-racket-oo) — at
  `generation/crates/emit-racket-oo/`
- `runtime/` — Racket runtime modules (7 files)
- `generated/` — output (regenerated by emitter, don't edit)
- `apps/` — sample app implementations
- `tests/` — Racket-side smoke tests (54 tests)
- `test-results/` — TestAnyware evidence (screenshots, reports)
- `lib/` — Swift dylib symlink
