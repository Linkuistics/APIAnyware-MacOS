# Modaliser — Configuration Patterns

Language-independent configuration design learnings. The configuration approach
is adapted per target language — executable config for Scheme-family targets,
potentially DSLs, builder patterns, or YAML+lambdas for others.
