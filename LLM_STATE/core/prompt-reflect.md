Read {{DEV_ROOT}}/LLM_CONTEXT/backlog-plan.md for the phase cycle spec
(focus on Phase 2: REFLECT).

Read {{PLAN}}/latest-session.md.
Read {{PLAN}}/memory.md — the current distilled learnings.

Distill learnings from the latest session into memory.md: add new entries,
sharpen existing ones, remove redundant or outdated ones.
Write triage to {{PLAN}}/phase.md. Then stop.

# --- Memory style (added by migrate-plan.sh) ---
When writing new memory entries or updating existing ones, follow the Memory
style rules from {{DEV_ROOT}}/LLM_CONTEXT/backlog-plan.md: assertion register
(not narrative), one fact per entry, cross-reference over re-explanation,
short subject-predicate headings, no session numbers or dates.
