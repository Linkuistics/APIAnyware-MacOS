### Session 20 (2026-04-18T10:27:56Z) — Mini Browser VNC verification + nsscreen.rkt triage

- Attempted full VNC verification of the Mini Browser app (4 checks: address-bar typing, link navigation, error alert, window resize), plus triage of the stale `nsscreen.rkt` duplicate-definition backlog entry.
- All four VNC checks PASSED on a fresh Tahoe VM (`guivision-00b7b26c`). The orphan `mini-browser-test` VM from the prior session had no recoverable VNC password (started outside `vm-start.sh`), so a fresh VM was spun up via `scripts/macos/vm-start.sh --platform macos`. Racket was reinstalled via `brew install minimal-racket` (detached `nohup`, ~5 min). Check 1 (address-bar typing): triple-click at screen-absolute (564, 99) + `guivision input type` + `input key return` navigated to example.com, Back enabled, heading visible. Check 2 (link navigation): VNC click on "Learn more" loaded iana.org/help/example-domains, window title updated, Back remained enabled. Check 3 (error alert): typing `not-a-url` surfaced NSAlert "A server with the specified hostname could not be found." — `WKNavigationDelegate` error path confirmed. Check 4 (window resize): 800×632 → 500×400 and → 900×667 both reflowed cleanly; toolbar controls remained visible.
- `nsscreen.rkt` triage: fix already landed in commit `1d03ede` (2026-04-18). Harness reported `OK: 25 library load checks passed`. No code changes needed; stale memory note removed. Task marked `done`.
- New task added to backlog: "Accessibility set-value fails for NSTextField inside NSStackView" — `guivision agent set-value` silently fails for textfields hosted in NSStackView; confirmed workaround is triple-click VNC + `guivision input type`.
- Three non-obvious testing findings recorded in `memory.md`: (a) `--window <name>` on `guivision input click` mis-translates AX origin due to shadow/chrome (~40 px offset) — use screen-absolute coords from full-screen screenshot; (b) triple-click reliably focuses+selects NSTextField hosted in NSStackView; (c) orphan tart VMs started outside `vm-start.sh` have no recoverable VNC password — must restart via the script.
- No source files were modified this session; all changes are confined to LLM_STATE plan files (backlog.md, memory.md, phase.md, work-baseline).
- Next: Note Editor sample app (NSSplitView + NSTextView + WKWebView loadHTMLString, completion blocks, NSUndoManager) — new territory for completion-block plumbing.
