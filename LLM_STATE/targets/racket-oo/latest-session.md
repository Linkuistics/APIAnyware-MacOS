### Session 20 (2026-04-16T08:05:10Z) — C-function type mapping fixes + emitter bug fixes

- **Attempted:** Four classes of C-function type mapping fixes to enable Modaliser-Racket FFI elimination; three emitter bug fixes (NSScreen duplicate define, NSMenuItem class-method suppression, WKWebView missing setAutoresizingMask:).
- **What worked:**
  - **Boolean → `_bool`:** Added `"Boolean"` to the well-known typedef list in `map_typedef` (`collection/crates/extract-objc/src/type_mapping.rs`). Fixes silent `_uint8` truthiness hazard for C `Boolean`-typed returns.
  - **`const char *` → `_string`:** Added `CString` TypeRefKind variant to the IR schema (`collection/crates/types/src/type_ref.rs`). ObjC extractor detects `char *`/`const char *` pointees via `is_c_string_pointee`; FFI mapper emits `_string`, contract mapper emits `string?`. New `extract_applicationservices` test coverage added.
  - **Framework-prefixed alias → integer (not `_id`):** Replaced fragile prefix-list heuristic in `ffi_type_mapping.rs` with `is_generic_type_param()`, which distinguishes ObjC generic type params (single uppercase then lowercase, e.g. "ObjectType") from framework-prefixed aliases (2+ uppercase chars, e.g. "AXValueType"). 2 new unit tests confirm the boundary.
  - **CF struct globals → `ffi-obj-ref`:** Changed `map_typedef` to emit `TypeRefKind::Struct` (not `Alias`) for struct typedefs. `is_struct_data_symbol` in `emit_constants.rs` now catches CF struct globals (`kCFTypeDictionaryKeyCallBacks` etc.). 1 new test.
  - **NSScreen duplicate `define` fix:** `effective_properties` in `emit_class.rs` now deduplicates by generated Racket getter name (not raw IR property name), closing the ObjC/Swift casing collision (`CGDirectDisplayID` vs `cgDirectDisplayID` → same kebab-case name). 4 new tests; golden files for `constants.rkt` and `enums.rkt` updated.
  - **NSMenuItem `+separatorItem` suppression fix:** `method_collides_with_property` now uses partitioned property name sets (class-level vs instance-level), so class methods only collide with class property names. Instance properties whose getter collides with a class method are suppressed (class method wins). 4 new tests; `nsmenuitem.rkt` golden file updated.
  - **WKWebView `setAutoresizingMask:` helper:** Created `generation/targets/racket-oo/runtime/nsview-helpers.rkt` with `set-autoresizing-mask!` using a typed `objc_msgSend` binding. Added to `RUNTIME_FILES` and `LIBRARY_LOAD_CHECKS` in the harness.
  - All 4 type-mapping fixes need a re-collect to propagate to the IR before they affect generated output.
  - Golden files for Foundation (`constants.rkt`, `enums.rkt`, `nsfilemanager.rkt`, `nsstring.rkt`, `nsurl.rkt`) and AppKit updated to reflect the fixes.
- **What didn't work / gaps:** The 3 collection-side fixes (Boolean, CString, Struct typedef) are code-complete but gated on re-collect; they won't be visible in generated output until the pipeline reruns against the updated extractor. No re-collect was performed this session.
- **What to try next:** Run full pipeline re-collect so Boolean/CString/Struct typedef fixes propagate to generated IR and bindings. Verify Modaliser-Racket can replace hand-written `get-ffi-obj` definitions with generated imports. Consider starting `is_definition()` guard audit in extractors (next bug fix task).
- **Key learnings:** The ObjC/Swift dual-extraction path creates casing collisions that are invisible until two properties kebab-case to the same name — deduplication must happen at the Racket-name level, not the IR level. The `is_generic_type_param` heuristic (single uppercase prefix) is more robust than a maintained allowlist for distinguishing ObjC generic type params from framework-prefixed enum typedefs.
